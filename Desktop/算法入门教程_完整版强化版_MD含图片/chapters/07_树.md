# 树

## 什么是树

树是一种非线性数据结构，由节点和边组成，具有层次结构。树结构模拟了现实中的层次关系，如文件系统、组织架构等。

### 树的基本特性

1. **层次结构**：节点之间存在父子关系
2. **唯一根节点**：每个树有且仅有一个根节点
3. **无环结构**：节点之间不存在环路
4. **递归定义**：子树也是树结构

### 树的基本概念

- **根节点**：树的顶层节点，没有父节点
- **叶子节点**：没有子节点的节点
- **内部节点**：既有父节点又有子节点的节点
- **父节点**：直接连接到下层节点的节点
- **子节点**：直接连接到上层节点的节点
- **兄弟节点**：具有相同父节点的节点
- **节点深度**：从根节点到该节点的边数
- **节点高度**：从该节点到最远叶子节点的边数
- **树的高度**：根节点的高度
- **树的度**：节点中最大的子节点数

## 二叉树

### 什么是二叉树

二叉树是每个节点最多有两个子节点的树结构，分别称为左子节点和右子节点。

### 二叉树的特性

1. **每个节点最多有两个子节点**
2. **子节点有左右之分**
3. **第 i 层最多有 2^(i-1) 个节点**
4. **高度为 h 的二叉树最多有 2^h - 1 个节点**

### 二叉树的实现

```python
class TreeNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None

class BinaryTree:
    def __init__(self):
        self.root = None
    
    def insert(self, val):
        """插入节点（按层次插入）"""
        if not self.root:
            self.root = TreeNode(val)
            return
        
        queue = [self.root]
        while queue:
            node = queue.pop(0)
            
            if not node.left:
                node.left = TreeNode(val)
                return
            else:
                queue.append(node.left)
            
            if not node.right:
                node.right = TreeNode(val)
                return
            else:
                queue.append(node.right)
```

### 二叉树的遍历

#### 前序遍历（根-左-右）

```python
def preorder_traversal(root):
    """前序遍历：根 -> 左 -> 右"""
    if not root:
        return []
    
    result = []
    stack = [root]
    
    while stack:
        node = stack.pop()
        result.append(node.val)
        
        # 先压入右子节点，再压入左子节点
        if node.right:
            stack.append(node.right)
        if node.left:
            stack.append(node.left)
    
    return result

# 递归版本
def preorder_recursive(root):
    """前序遍历递归版本"""
    if not root:
        return []
    
    return [root.val] + preorder_recursive(root.left) + preorder_recursive(root.right)
```

#### 中序遍历（左-根-右）

```python
def inorder_traversal(root):
    """中序遍历：左 -> 根 -> 右"""
    if not root:
        return []
    
    result = []
    stack = []
    current = root
    
    while current or stack:
        # 遍历到最左边的节点
        while current:
            stack.append(current)
            current = current.left
        
        current = stack.pop()
        result.append(current.val)
        current = current.right
    
    return result

# 递归版本
def inorder_recursive(root):
    """中序遍历递归版本"""
    if not root:
        return []
    
    return inorder_recursive(root.left) + [root.val] + inorder_recursive(root.right)
```

#### 后序遍历（左-右-根）

```python
def postorder_traversal(root):
    """后序遍历：左 -> 右 -> 根"""
    if not root:
        return []
    
    result = []
    stack = [root]
    
    while stack:
        node = stack.pop()
        result.append(node.val)
        
        if node.left:
            stack.append(node.left)
        if node.right:
            stack.append(node.right)
    
    return result[::-1]

# 递归版本
def postorder_recursive(root):
    """后序遍历递归版本"""
    if not root:
        return []
    
    return postorder_recursive(root.left) + postorder_recursive(root.right) + [root.val]
```

#### 层次遍历（广度优先）

```python
from collections import deque

def level_order_traversal(root):
    """层次遍历（广度优先）"""
    if not root:
        return []
    
    result = []
    queue = deque([root])
    
    while queue:
        level_size = len(queue)
        level_nodes = []
        
        for _ in range(level_size):
            node = queue.popleft()
            level_nodes.append(node.val)
            
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
        
        result.append(level_nodes)
    
    return result
```

## 二叉搜索树

### 什么是二叉搜索树

二叉搜索树（BST）是一种特殊的二叉树，对于每个节点：
- 左子树的所有节点值都小于该节点值
- 右子树的所有节点值都大于该节点值
- 左右子树也都是二叉搜索树

### 二叉搜索树的实现

```python
class BSTNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None

class BinarySearchTree:
    def __init__(self):
        self.root = None
    
    def insert(self, val):
        """插入节点"""
        if not self.root:
            self.root = BSTNode(val)
            return
        
        self._insert_recursive(self.root, val)
    
    def _insert_recursive(self, node, val):
        """递归插入"""
        if val < node.val:
            if node.left is None:
                node.left = BSTNode(val)
            else:
                self._insert_recursive(node.left, val)
        else:
            if node.right is None:
                node.right = BSTNode(val)
            else:
                self._insert_recursive(node.right, val)
    
    def search(self, val):
        """查找节点"""
        return self._search_recursive(self.root, val)
    
    def _search_recursive(self, node, val):
        """递归查找"""
        if not node or node.val == val:
            return node
        
        if val < node.val:
            return self._search_recursive(node.left, val)
        else:
            return self._search_recursive(node.right, val)
    
    def delete(self, val):
        """删除节点"""
        self.root = self._delete_recursive(self.root, val)
    
    def _delete_recursive(self, node, val):
        """递归删除"""
        if not node:
            return None
        
        if val < node.val:
            node.left = self._delete_recursive(node.left, val)
        elif val > node.val:
            node.right = self._delete_recursive(node.right, val)
        else:
            # 找到要删除的节点
            # 情况1：叶子节点
            if not node.left and not node.right:
                return None
            # 情况2：只有一个子节点
            elif not node.left:
                return node.right
            elif not node.right:
                return node.left
            # 情况3：有两个子节点
            else:
                # 找到右子树的最小值节点
                min_node = self._find_min(node.right)
                node.val = min_node.val
                node.right = self._delete_recursive(node.right, min_node.val)
        
        return node
    
    def _find_min(self, node):
        """找到最小值节点"""
        while node.left:
            node = node.left
        return node
```

### 二叉搜索树的操作复杂度

| 操作 | 平均情况 | 最坏情况 |
|------|----------|----------|
| 查找 | O(log n) | O(n) |
| 插入 | O(log n) | O(n) |
| 删除 | O(log n) | O(n) |

## 平衡二叉树

### 为什么需要平衡二叉树

普通二叉搜索树在最坏情况下会退化为链表，导致操作复杂度变为 O(n)。平衡二叉树通过保持树的平衡来确保操作效率。

### AVL树

AVL树是一种自平衡二叉搜索树，对于每个节点，其左右子树的高度差不超过1。

```python
class AVLNode:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
        self.height = 1

class AVLTree:
    def __init__(self):
        self.root = None
    
    def height(self, node):
        """获取节点高度"""
        if not node:
            return 0
        return node.height
    
    def balance_factor(self, node):
        """计算平衡因子"""
        if not node:
            return 0
        return self.height(node.left) - self.height(node.right)
    
    def right_rotate(self, y):
        """右旋转"""
        x = y.left
        T2 = x.right
        
        x.right = y
        y.left = T2
        
        y.height = max(self.height(y.left), self.height(y.right)) + 1
        x.height = max(self.height(x.left), self.height(x.right)) + 1
        
        return x
    
    def left_rotate(self, x):
        """左旋转"""
        y = x.right
        T2 = y.left
        
        y.left = x
        x.right = T2
        
        x.height = max(self.height(x.left), self.height(x.right)) + 1
        y.height = max(self.height(y.left), self.height(y.right)) + 1
        
        return y
    
    def insert(self, val):
        """插入节点"""
        self.root = self._insert(self.root, val)
    
    def _insert(self, node, val):
        """递归插入并平衡"""
        if not node:
            return AVLNode(val)
        
        if val < node.val:
            node.left = self._insert(node.left, val)
        else:
            node.right = self._insert(node.right, val)
        
        node.height = max(self.height(node.left), self.height(node.right)) + 1
        
        balance = self.balance_factor(node)
        
        # 左左情况
        if balance > 1 and val < node.left.val:
            return self.right_rotate(node)
        
        # 右右情况
        if balance < -1 and val > node.right.val:
            return self.left_rotate(node)
        
        # 左右情况
        if balance > 1 and val > node.left.val:
            node.left = self.left_rotate(node.left)
            return self.right_rotate(node)
        
        # 右左情况
        if balance < -1 and val < node.right.val:
            node.right = self.right_rotate(node.right)
            return self.left_rotate(node)
        
        return node
```

## 树的应用

### 应用1：表达式树

```python
class ExpressionTree:
    def __init__(self):
        self.root = None
    
    def build(self, postfix):
        """根据后缀表达式构建表达式树"""
        stack = []
        
        for token in postfix:
            if token.isdigit():
                stack.append(TreeNode(int(token)))
            else:
                node = TreeNode(token)
                node.right = stack.pop()
                node.left = stack.pop()
                stack.append(node)
        
        self.root = stack.pop()
    
    def evaluate(self):
        """计算表达式树的值"""
        return self._evaluate(self.root)
    
    def _evaluate(self, node):
        """递归计算"""
        if not node:
            return 0
        
        if not node.left and not node.right:
            return int(node.val)
        
        left_val = self._evaluate(node.left)
        right_val = self._evaluate(node.right)
        
        if node.val == '+':
            return left_val + right_val
        elif node.val == '-':
            return left_val - right_val
        elif node.val == '*':
            return left_val * right_val
        elif node.val == '/':
            return left_val // right_val
    
    def inorder(self):
        """中序遍历（带括号）"""
        return self._inorder(self.root)
    
    def _inorder(self, node):
        """递归中序遍历"""
        if not node:
            return ""
        
        if not node.left and not node.right:
            return str(node.val)
        
        left = self._inorder(node.left)
        right = self._inorder(node.right)
        
        return f"({left}{node.val}{right})"

# 使用示例
expr_tree = ExpressionTree()
expr_tree.build(["3", "4", "+", "2", "*"])
print(expr_tree.inorder())    # ((3+4)*2)
print(expr_tree.evaluate())   # 14
```

### 应用2：文件系统

```python
class FileNode:
    def __init__(self, name, is_file=False, size=0):
        self.name = name
        self.is_file = is_file
        self.size = size
        self.children = {}  # 子文件或文件夹

class FileSystem:
    def __init__(self):
        self.root = FileNode("", is_file=False)
    
    def create_path(self, path, value=0):
        """创建文件路径"""
        components = path.split('/')
        node = self.root
        
        for i, component in enumerate(components[:-1]):
            if component not in node.children:
                return False
            node = node.children[component]
        
        file_name = components[-1]
        if file_name in node.children:
            return False
        
        is_file = i == len(components) - 2
        node.children[file_name] = FileNode(file_name, is_file, value)
        return True
    
    def get(self, path):
        """获取文件大小"""
        components = path.split('/')
        node = self.root
        
        for component in components:
            if component not in node.children:
                return -1
            node = node.children[component]
        
        return node.size if node.is_file else -1

# 使用示例
fs = FileSystem()
fs.create_path("/a", 1)
print(fs.get("/a"))  # 1
```

### 应用3：字典树（Trie）

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()
    
    def insert(self, word):
        """插入单词"""
        node = self.root
        for char in word:
            if char not in node.children:
                node.children[char] = TrieNode()
            node = node.children[char]
        node.is_end = True
    
    def search(self, word):
        """搜索单词"""
        node = self.root
        for char in word:
            if char not in node.children:
                return False
            node = node.children[char]
        return node.is_end
    
    def starts_with(self, prefix):
        """检查是否有前缀"""
        node = self.root
        for char in prefix:
            if char not in node.children:
                return False
            node = node.children[char]
        return True

# 使用示例
trie = Trie()
trie.insert("apple")
print(trie.search("apple"))   # True
print(trie.search("app"))     # False
print(trie.starts_with("app"))  # True
```

### 应用4：霍夫曼编码

```python
import heapq
from collections import Counter

class HuffmanNode:
    def __init__(self, char=None, freq=0, left=None, right=None):
        self.char = char
        self.freq = freq
        self.left = left
        self.right = right
    
    def __lt__(self, other):
        return self.freq < other.freq

def build_huffman_tree(text):
    """构建霍夫曼树"""
    if not text:
        return None
    
    # 统计字符频率
    freq = Counter(text)
    
    # 创建优先队列
    heap = [HuffmanNode(char, freq) for char, freq in freq.items()]
    heapq.heapify(heap)
    
    # 构建霍夫曼树
    while len(heap) > 1:
        left = heapq.heappop(heap)
        right = heapq.heappop(heap)
        merged = HuffmanNode(freq=left.freq + right.freq, left=left, right=right)
        heapq.heappush(heap, merged)
    
    return heap[0]

def generate_codes(node, current_code="", code_map=None):
    """生成霍夫曼编码"""
    if code_map is None:
        code_map = {}
    
    if node is None:
        return code_map
    
    if node.char is not None:
        code_map[node.char] = current_code
        return code_map
    
    generate_codes(node.left, current_code + "0", code_map)
    generate_codes(node.right, current_code + "1", code_map)
    
    return code_map

# 使用示例
text = "hello world"
huffman_tree = build_huffman_tree(text)
huffman_codes = generate_codes(huffman_tree)
print(huffman_codes)
```

## 树的常见问题

### 问题1：二叉树的最大深度

```python
def max_depth(root):
    """计算二叉树的最大深度"""
    if not root:
        return 0
    
    left_depth = max_depth(root.left)
    right_depth = max_depth(root.right)
    
    return max(left_depth, right_depth) + 1
```

### 问题2：判断是否为平衡二叉树

```python
def is_balanced(root):
    """判断是否为平衡二叉树"""
    def check_balance(node):
        if not node:
            return 0
        
        left_height = check_balance(node.left)
        if left_height == -1:
            return -1
        
        right_height = check_balance(node.right)
        if right_height == -1:
            return -1
        
        if abs(left_height - right_height) > 1:
            return -1
        
        return max(left_height, right_height) + 1
    
    return check_balance(root) != -1
```

### 问题3：二叉树的最近公共祖先

```python
def lowest_common_ancestor(root, p, q):
    """查找最近公共祖先"""
    if not root or root == p or root == q:
        return root
    
    left = lowest_common_ancestor(root.left, p, q)
    right = lowest_common_ancestor(root.right, p, q)
    
    if left and right:
        return root
    
    return left if left else right
```

### 问题4：将有序数组转换为二叉搜索树

```python
def sorted_array_to_bst(nums):
    """将有序数组转换为平衡二叉搜索树"""
    def convert(left, right):
        if left > right:
            return None
        
        mid = (left + right) // 2
        node = TreeNode(nums[mid])
        node.left = convert(left, mid - 1)
        node.right = convert(mid + 1, right)
        
        return node
    
    return convert(0, len(nums) - 1)
```

## 练习题

1. 实现一个函数，计算二叉树的直径（任意两个节点之间的最长路径）。
2. 给定二叉树的前序遍历和中序遍历，重构该二叉树。
3. 实现一个函数，判断二叉树是否对称。
4. 设计一个算法，找出二叉树中第 k 小的元素。

## 总结

树是一种重要的非线性数据结构：

- **基本结构**：层次结构，根节点、叶子节点、子树
- **二叉树**：每个节点最多两个子节点
- **二叉搜索树**：左小右大，支持高效查找
- **平衡树**：保持平衡，确保操作效率
- **广泛应用**：文件系统、表达式求值、字典树等

掌握树的各种遍历方法和操作，为解决复杂问题提供有力工具。

下一章我们将学习堆，这是一种特殊的树结构，常用于优先队列和排序算法。
